<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.reply">
	
	<!-- 댓글삭제 -->
	<delete id="delete" parameterType="java.lang.String">
		delete from reply where reply_idx=#{reply_idx}
	</delete>
	
	<!-- 댓글 리스트 -->
	<select id="list" resultType="com.web.f5.board.vo.ReplyVO" parameterType="java.lang.String">
		select reply_idx, board_idx, member_id, reply_content, reply_date, reply_update_date
			from reply where board_idx=#{board_idx}
	</select>
	
	<!-- 댓글입력 -->
	<insert id="insert" parameterType="com.web.f5.board.vo.ReplyVO">
		insert into reply 
			values('r_'||sequ_reply_idx.nextval, 'b_16', 'test', 
					#{reply_content}, sysdate, #{reply_update_date, jdbcType=VARCHAR})
	</insert>
	
</mapper>